import 'package:flutter/material.dart';

class VehicleOwnershipScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vehicle Ownership"),
      ),
      body: Text("vehicle Ownership Screen"),
    );
  }
}
