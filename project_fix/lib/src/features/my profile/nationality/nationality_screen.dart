import 'package:flutter/material.dart';

class NationalityScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Nationality"),
      ),
      body: Text("Nationality Screen"),
    );
  }
}
