import 'package:flutter/material.dart';

class RealNameAuthentication extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Real Name Authentication"),
      ),
      body: Text("Real Name Authentication Screen"),
    );
  }
}
